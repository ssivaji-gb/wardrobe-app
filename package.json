{
  "name": "wardrobe-management-app",
  "version": "1.0.0",
  "description": "Wardrobe Management Application Mock Server",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
